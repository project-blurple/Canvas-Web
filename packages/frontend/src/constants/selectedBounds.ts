import type { BoundsDimensions } from "@/util";

export const COMPLEX_SEARCH_BOUNDS_MIN_SIZE: BoundsDimensions = {
	width: 1,
	height: 1,
};

export const SELECTED_BOUNDS_DEFAULT_MIN_SIZE: BoundsDimensions = {
	width: 5,
	height: 5,
};